# OceanTrace — Indhu (detection ML + main system)
#
# PyTorch is NOT listed here: the CUDA build must come from PyTorch's own
# index, and a plain `pip install torch` silently installs the CPU wheel,
# which cannot train. Install it first:
#
#   python -m venv .venv
#   .venv/Scripts/python -m pip install torch torchvision \
#       --index-url https://download.pytorch.org/whl/cu121
#   .venv/Scripts/python -m pip install -r 1_indhu_main_system/requirements.txt
#
# Verify before training:  python -c "import torch; print(torch.cuda.is_available())"

# --- detection ML ---------------------------------------------------------
segmentation-models-pytorch>=0.3.4   # U-Net + ResNet-34 ImageNet encoder
albumentations>=1.4                  # geometric/radiometric augmentation
ultralytics>=8.2                     # YOLO screening model (DARTIS stage)
onnx>=1.16                           # export
onnxruntime-gpu>=1.18                # inference in the /detect service

# --- geospatial / raster --------------------------------------------------
rasterio>=1.3                        # GeoTIFF read/write, keeps georeferencing
scikit-image>=0.22                   # regionprops + threshold_fallback path
tifffile>=2024.1                     # rasterio-hostile TIFF fallback
opencv-python-headless>=4.9
shapely>=2.0
pyproj>=3.6

# --- backend / API --------------------------------------------------------
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6                        # contract validation
sqlalchemy>=2.0
python-multipart>=0.0.9
httpx>=0.27

# --- contracts ------------------------------------------------------------
# vessels.parquet is a frozen contract; reading/validating it needs both.
pandas>=2.2
pyarrow>=15.0

# --- shared ---------------------------------------------------------------
numpy>=1.26
PyYAML>=6.0                          # config/normalisation.yaml
tqdm>=4.66
requests>=2.31
matplotlib>=3.8                      # audit contact sheets, metric plots

# --- testing --------------------------------------------------------------
pytest>=8.0
